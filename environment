export cxgf_home="$(cd -P "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export CELLXGENE_IMAGE=gi.ucsc.edu/cellxgene
export CELLXGENE_VERSION=0.14.1
export PYTHONPATH="${cxgf_home}/src"
export AWS_DEFAULT_REGION=us-east-1

CXG_DATA_URL='https://cellxgene-example-data.czi.technology/pbmc3k.h5ad'

if test -f environment.local; then source environment.local; fi
